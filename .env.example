OLLI_API_KEY=olli_sk_your_key_here
OLLI_BASE_URL=https://api.olli.social/v1

# Schema sync — used by `npm run sync-schemas` to fetch avatar tool definitions
# from the olli-social-app Rails server (AvatarToolRegistry source of truth).
MCP_RAILS_URL=https://api.olli.social
MCP_SHARED_SECRET=your_shared_secret_here
